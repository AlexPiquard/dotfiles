nerdfont: true

# let the tmux theme own window-status-format, otherwise workmux's uncolored one wins
status_format: false

agent: opencode
agents:
  opencode:
    type: opencode
    command: opencode --port

panes:
  - command: <agent>
  - command: nvim
    split: horizontal
    percentage: 70
    focus: true
